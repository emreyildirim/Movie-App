const SearchYear = (props) => {
    return (
      <input
         className="search-year"
         placeholder="Year"
         onChange={props.onChange}
      ></input>
    );
  };
  export default SearchYear;
  